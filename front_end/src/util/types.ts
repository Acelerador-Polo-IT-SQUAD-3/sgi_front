export type Role = {
    id: number;
    name: string;
};

export type Program = {
    id: number;
    name: string;
    description: string;
};

export type Technology = {
    id: number;
    name: string;
};